mainmodule main {

  readonly CProxy_Main mainProxy;
  readonly int numElements;

  extern module kmeans;

  mainchare Main {
    entry Main(CkArgMsg* msg);
    entry void done();
    entry void finished(const std::vector<double>&, int);
  };

};